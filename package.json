{
  "name": "front-package",
  "version": "1.0.0",
  "workspaces": [
    "packages/**/*",
    "services/**/*"
  ],
  "scripts": {
    "build": "nx build",
    "graph":"nx run-many --target=build --graph"
  },
  "devDependencies": {
    "nx": "^16.8.1"
  }
}
